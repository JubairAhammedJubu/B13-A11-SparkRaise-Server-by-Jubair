# MongoDB connection string (same database the frontend's Better Auth connects to)
MONGODB_URI=

# Port the Express server listens on (defaults to 8000 if unset)
PORT=8000

# Frontend origin, used only if you uncomment the strict CORS config in index.js
CLIENT_URL=http://localhost:3000
